mvn spring-boot:run
第一次运行先安装依赖（下载慢就加淘宝源）：
npm install --registry=https://registry.npmmirror.com
npm run dev


1. 启动 Docker 和 Chroma 容器：

# 查看 Chroma 容器名
docker ps -a

# 启动已有容器（把 <容器名> 换成实际名字）
docker start <容器名>

# 如果容器不存在，重新创建
docker run -d -p 8000:8000 --name chroma chromadb/chroma

#停止
docker stop chroma

#删除
docker rm chroma


2.  Ollama：
#启动
如果安装了 Ollama 桌面端，直接打开应用即可

3.Redis
redis-cli ping

